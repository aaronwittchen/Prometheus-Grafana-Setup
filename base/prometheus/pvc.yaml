apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prometheus-storage
  labels:
    app: prometheus
spec:
  accessModes:
    - ReadWriteOnce
  # Storage class will be set via Kustomize overlay
  resources:
    requests:
      storage: 25Gi
